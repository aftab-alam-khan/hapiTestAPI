version: '2.1'

services:
  mongodb:
    image: mongo:latest
    container_name: "mongodb"
    environment:
      - MONGO_DATA_DIR=/data/db
      - MONGO_LOG_DIR=/dev/null
    volumes:
      - ./data/db:/data/db
    ports:
        - 27017:27017
    command: mongod --smallfiles --logpath=/dev/null 
  # api:
  #   image: pager-code-test
  #   build: .
  #   environment:
  #     NODE_ENV: production
  #   ports:
  #     - 3000:3000
  #   links:
  #     - mongodb
  #   depends_on:
  #     - mongodb
